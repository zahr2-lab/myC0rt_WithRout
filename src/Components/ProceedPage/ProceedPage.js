import React from "react";

export default function Proceed() {
  return (
    <div>
      <div>Select Address</div>
      <select>
        <options>first Address</options>
      </select>
      <div>Select Payment</div>
      <select>
        <options>first Address</options>
      </select>
    </div>
  );
}
